{
  "name": "indocoin-auto-volume-sync",
  "version": "1.0.0",
  "description": "Auto sync staking volume to Aggregator using dev wallet",
  "main": "auto-volume-sync.js",
  "scripts": {
    "start": "node auto-volume-sync.js"
  },
  "dependencies": {
    "ethers": "^5.7.2",
    "dotenv": "^16.4.5"
  }
}
